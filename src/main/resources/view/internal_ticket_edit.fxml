<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.app.controller.InternalTicketEditController"
            style="-fx-background-color:#f8fafc;">

    <center>

        <VBox spacing="20"
              minWidth="0"
              maxWidth="Infinity"
              alignment="TOP_CENTER"
              style="-fx-padding:30;">

            <Label text="%editInternalTicket"
                   style="-fx-font-size:22px; -fx-font-weight:bold;"/>

            <VBox spacing="15"
                  minWidth="280"
                  maxWidth="Infinity"
                  style="-fx-background-color:white;
                         -fx-padding:25;
                         -fx-background-radius:10;
                         -fx-effect:dropshadow(gaussian,rgba(0,0,0,0.1),10,0,0,4);">

                <VBox spacing="5">
                    <Label text="%title"/>
                    <TextField fx:id="txtTitle"/>
                </VBox>

                <VBox spacing="5">
                    <Label text="%description"/>
                    <TextArea fx:id="txtDescription"
                              prefRowCount="4"/>
                </VBox>

                <VBox spacing="5">
                    <Label text="%priority"/>
                    <ComboBox fx:id="cmbPriority"/>
                </VBox>

                <VBox spacing="5">
                    <Label text="%department"/>
                    <ComboBox fx:id="cmbDepartment"/>
                </VBox>

                <HBox spacing="10" alignment="CENTER_RIGHT">

                    <Button text="%cancel"
                            onAction="#handleCancel"
                            style="-fx-background-color:#e5e7eb;"/>

                    <Button text="%updateTicket"
                            onAction="#handleUpdate"
                            style="-fx-background-color:#2f80ed;
                                   -fx-text-fill:white;"/>

                </HBox>

            </VBox>

        </VBox>

    </center>

</BorderPane>